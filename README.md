# Overlab

Bodybuilding progress tracker & analytics solution.

## Change history

- Adjust README
- `ng new overlab --prefix olab --directory overlab --style scss --defaults true --package-manager pnpm --file-name-style-guide 2016`
- Copy LICENSE (MIT)
- Remove tutorial comments from all the files
- `.editorconfig` - override `*.ts` to have `4` indent size
- Update `favicon`
- Add `"strictTemplates": true` to `AngularCompilerOptions` for strict template checking
- Fix tests if needed
- Run e2e tests - install Playwright
- Edit Playwright config, comment out webkit (for Arch), remove comments
- Adjust the e2e test
- Add /.vscode/ to .gitignore
- Initial commit
